# SDD ledger — plan: docs/superpowers/plans/2026-09-24-facet-aware-semantic-retrieval.md
Workspace: current main checkout, user explicitly authorized implementation on main.
Pre-flight: shared interfaces checked — Task 1 packets → Task 2 decomposer; Task 2 needs → Task 3 retrieval; Task 3 retrieval trace → Task 4 relaxation; Task 4 treatment → Task 5 KnowledgeUnit/selector; Task 5 pack → Task 6 adapters; Task 6 diagnostics → Task 7 performance; Task 7 verification → Task 8 docs/state.
Ruling: work directly on main — user explicitly authorized it; cost if wrong: changes require manual separation/revert before integration.
Task 1: RED confirmed — missing packet classes produced expected PHPUnit errors/failures. GREEN confirmed — `SemanticNeedDecompositionTest` 6 tests / 18 assertions passed; PHP lint and diff check passed. Test adjustment used `array_replace` so the missing-subject case actually overrides the base fixture.
Task 1: complete (commits e103a756..d9b642ee, tests: `vendor/bin/phpunit -c phpunit.xml.dist --testsuite 'NHK Unit' --filter 'SemanticNeedDecompositionTest' --no-progress` → 6/6 pass).
Task 2: RED confirmed — decomposer tests failed because `SemanticNeedDecomposer` was absent. GREEN confirmed — decomposition suite passed 9 tests / 30 assertions; existing `EditorialCaptureSemanticCoreTest` passed 34 tests / 135 assertions; PHP lint and diff check passed. Ruling: the current repository has no registered facet vocabulary adapter to inject, so the decomposer accepts a generic `isRegistered`/`has` read port and fails closed when absent; cost if wrong: a future registry adapter must implement one of those methods.
Task 2: complete (commits d9b642ee..f0055ef9, tests: `vendor/bin/phpunit -c phpunit.xml.dist --testsuite 'NHK Unit' --filter 'SemanticNeedDecompositionTest' --no-progress` → 9/9 pass).
Task 3: RED confirmed — new multi-need tests first failed on the missing engine method; a fixture closure syntax/context error was corrected before implementation. GREEN confirmed — retrieval/service/scope/expansion matrix passed 13 tests / 52 assertions; PHP lint and diff check passed. Ruling: legacy claim callbacks are arity-detected via Reflection so existing two-argument ports remain compatible while new three-argument need-aware ports are supported; cost if wrong: callable reflection behavior must remain compatible with all registered ports.
Task 3: complete (commits f0055ef9..dc42010c, tests: `vendor/bin/phpunit -c phpunit.xml.dist --testsuite 'NHK Unit' --filter 'FacetAwareClaimRetrievalTest|EditorialClaimRetrievalServiceTest|ClaimRetrievalScopeTest|EditorialClaimRetrievalExpansionTest' --no-progress` → 13/13 pass).
Task 4: RED confirmed — relaxation tests initially failed because no per-need tier loop existed; the first test fixture also had an invalid arrow-function capture and was corrected. GREEN confirmed — relaxation/retrieval regression matrix passed 17 tests / 64 assertions; PHP lint and diff check passed. Ruling: use `applicable_relaxed` as the machine-readable coverage kind for safe non-exact candidates and `uncovered` for exhausted/unsafe tiers; cost if wrong: downstream consumers must treat this vocabulary as the current transient contract.
Task 4: complete (commits dc42010c..f4ed7c67, tests: `vendor/bin/phpunit -c phpunit.xml.dist --testsuite 'NHK Unit' --filter 'SemanticRelaxationTest|FacetAwareClaimRetrievalTest|EditorialClaimRetrievalServiceTest|ClaimRetrievalScopeTest|EditorialClaimRetrievalExpansionTest' --no-progress` → 17/17 pass).
Task 5: RED confirmed — new selector coverage diagnostic assertion failed because `coverage_kinds` was absent; builder trace assertions and existing adaptive behavior remained green. GREEN confirmed — KnowledgeUnit/selector/journey/quality/relaxation matrix passed 16 tests / 71 assertions; PHP lint and diff check passed. Ruling: retain need/tier/treatment directly in the transient Claim/KnowledgeUnit trace and expose only bounded `coverage_kinds` in selector diagnostics; cost if wrong: downstream consumers must read transient diagnostic metadata rather than a new canonical field.
Task 5: complete (commits f4ed7c67..6866132c, tests: `vendor/bin/phpunit -c phpunit.xml.dist --testsuite 'NHK Unit' --filter 'KnowledgeUnitBuilderTest|EditorialKnowledgeSelectorAdaptiveTest|AdaptiveKnowledgeQualityDiagnosticsTest|ReaderJourneyCoverageTest|SemanticRelaxationTest' --no-progress` → 16/16 pass).
Task 6: RED/GREEN confirmed — shared boundary now accepts an optional registered-vocabulary decomposer, carries envelope components, routes decomposed or supplied needs through need-aware retrieval, and returns transient semantic-needs/decomposition diagnostics; article/video adapters preserve owner-specific profiles while forwarding needs. Existing uncommitted adapter/boundary/selector/retrieval-test changes were preserved and verified, not reverted. Focused shared/retrieval/adaptive matrix passed 88 tests / 378 assertions; PHP lint and diff check passed. Ruling: no generic vocabulary adapter is invented at Plugin wiring because the repository currently exposes no canonical facet/concept pair registry; production injection remains optional and fail-closed until a registered read port is available.
Task 6: complete (pending commit; tests: `vendor/bin/phpunit --filter 'SharedEnrichmentBoundaryTest|ArticleEditorialAdapterTest|VideoEditorialAdapterTest|VideoSemanticCoreTest|FacetAwareClaimRetrievalTest|SemanticNeedDecompositionTest|SemanticRelaxationTest|KnowledgeUnitBuilderTest|EditorialKnowledgeSelectorTest' public/wp-content/plugins/nhk-core/tests/Unit` → 88/88 pass).
Task 7: RED confirmed — diagnostics test failed on missing `initial_candidate_count`; GREEN confirmed — bounded counters now expose initial candidates, allocation, expansion rounds/depth, coverage counts, selected count and stop-reason counts without raw payloads. Performance/diagnostic suite passed twice identically at 2 tests / 18 assertions per run; broader regression subset passed 11 tests / 47 assertions. Special-case scan found only pre-existing identity/content-quality references in `CanonicalAuthoritySubjectResolver.php` and `EditorialQualityGate.php`; no new decomposition/retrieval special case was added. PHP lint and diff check passed.
Task 7: complete (pending commit; test command: `vendor/bin/phpunit --filter 'SemanticRetrievalDiagnosticsTest|FacetAwareClaimRetrievalTest|SemanticRelaxationTest|AdaptiveKnowledgeSelectionPerformanceTest' public/wp-content/plugins/nhk-core/tests/Unit` → 11/11 pass).
Task 8: complete — contract/state checkpoint recorded; focused matrix passed 379/379 tests and 1,563 assertions; Full Unit passed 2,446/2,446 tests and 14,126 assertions under 512M; Contract passed 6/6 tests and 48 assertions; guarded Integration attempted with exact `NHK_WP_TEST_PATH=public NHK_WP_TEST_DB=nhk_v3_test` but remains runtime-gated by WordPress database connection failure. Composer lint, changed-file PHP lint, diff check and secret review completed; no schema/migration/database/live mutation, deployment or push.
